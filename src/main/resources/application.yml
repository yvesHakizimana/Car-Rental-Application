spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/car_rental_db
    username: postgres
    password: P0S1tiv@!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
server:
  port: 9000